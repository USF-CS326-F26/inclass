[package]
name = "echo_example"
version = "0.1.0"
edition = "2021"
# so that a bare `cargo run` picks the demo, not src/bin/.
default-run = "echo_example"

# The worked example lives in src/lib.rs (with its own tests); src/main.rs is
# the live demo:
#   cargo test          # the contract, same shape as the 10c exercise
#   cargo run           # the narrated walkthrough to put on screen
#   cargo run -- a/b c  # ...or run it as the command itself
[dependencies]
